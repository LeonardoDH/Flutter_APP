import 'package:flutter/material.dart';

class IntrolPage2 extends StatelessWidget {
  const IntrolPage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purpleAccent[100],
      child: Center(child: Text('page 2!'),),
    );
  }
}
